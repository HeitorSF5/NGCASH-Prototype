version: "4"

services:
  db:
    image: "postgres:15"
    restart: "always"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "ngcashadmin"
      POSTGRES_PASSWORD: "secret"
      POSTGRES_DB: NGCASHPrototype